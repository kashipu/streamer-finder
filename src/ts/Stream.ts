class Stream {
    id: number;
    category: string[];
    temas: string[];

    constructor(id: number, category: string[], temas: string[]) {
        this.id = id;
        this.category = category;
        this.temas = temas;
    }
}

class Streamer {
    id: number;
    user: string;
    summary: string;

    constructor(id: number, user: string, summary: string) {
        this.id = id;
        this.user = user;
        this.summary = summary;
    }
}